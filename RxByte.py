__author__ = 'Cesar'


        # RxBusy= False
        # buffer = list()
        # buffer.append(0x00)
        # # Rx (Byte per Byte)
        # while buffer[-1] != 0x1A: #Get the last item on the list
        #     if port.inWaiting > 0:
        #         RxBusy = True
        #         buffer.append(port.read())
        #         print(port.read())
        #         #Get the last item on the list
        #         #MessageFromSerial = MessageFromSerial + buffer[-1]
        #     else:
        #         RxBusy = False
        # print ("Rx = {}".format(MessageFromSerial.decode()))
